import XCTest
@testable import LocalizableParser

final class LocalizableParserTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LocalizableParser().text, "Hello, World!")
    }
}
